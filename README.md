# SimpleChat
A minimal real-time chat application using Node.js, WebSockets, and vanilla JavaScript.

## Features
- Join and leave a chatroom with a username
- Send and receive messages in real time
- All messages broadcast to all connected users

### Installation
1. Clone or download this repository.
2. Open a terminal in the project folder.
3. Install dependencies: npm install

### Running the Server
Run a live server of index.html
Start the WebSocket server: node server.js
The server will run on `ws://localhost:8080` by default.

## Notes
- This project is for learning/demo purposes. No authentication or message persistence.
